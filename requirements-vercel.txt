# OPTIMIZED REQUIREMENTS FOR VERCEL DEPLOYMENT
# This file contains ONLY the minimal dependencies needed for the frontend
# Heavy ML dependencies should be used via APIs (OpenAI, Anthropic)

# Frontend Framework
streamlit>=1.30.0
plotly>=5.18.0

# Lightweight data handling
pandas>=2.0.0

# Backend Communication
httpx>=0.26.0
requests>=2.31.0

# Configuration
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Security (lightweight)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Utilities
python-dateutil>=2.8.2
pytz>=2023.3
pyyaml>=6.0.1

# Logging
loguru>=0.7.0
